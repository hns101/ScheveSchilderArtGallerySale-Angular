export const environment = {
  production: false,
  whatsappNumber: '31612345678' // Replace with actual WhatsApp number (country code + number, no + or spaces)
};

// src/environments/environment.prod.ts
export const environmentProd = {
  production: true,
  whatsappNumber: '31612345678' // Replace with actual WhatsApp number (country code + number, no + or spaces)
};
